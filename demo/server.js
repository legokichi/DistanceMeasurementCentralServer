// Generated by CoffeeScript 1.10.0
(function() {
  var api_router, app, bodyParser, express, formidable, io, log, logs, request, requestLinear, requestParallel, server, start;

  bodyParser = require('body-parser');

  formidable = require('formidable');

  express = require('express');

  app = express();

  server = require('http').Server(app);

  io = require('socket.io')(server);

  logs = [];

  log = function() {
    logs.push(Array.prototype.slice.call(arguments).join("\t"));
    return console.log.apply(console, arguments);
  };

  api_router = express.Router();

  api_router.get('/sockets', function(req, res) {
    return res.json(io.of('/node').sockets.map(function(a) {
      return a.id;
    }));
  });

  app.use(bodyParser.urlencoded({
    extended: true
  }));

  app.use(bodyParser.json());

  app.use('/api', api_router);

  app.use('/', express["static"](__dirname + '/../demo'));

  io.of('/node').on('connection', function(socket) {
    console.info("connection", socket.client.id);
    socket.on('echo', function(data) {
      return socket.emit("echo", data);
    });
    socket.on('event', console.info.bind(console, "event"));
    return socket.on('disconnect', console.info.bind(console, "disconnect"));
  });

  io.of('/calc').on('connection', function(socket) {
    socket.on('echo', function(data) {
      return socket.emit("echo", data);
    });
    socket.on('event', console.info.bind(console, "event"));
    socket.on('disconnect', console.info.bind(console, "disconnect"));
    socket.on("set_vs_weight", function(data) {
      return requestParallel("set_vs_weight", data).then(function() {
        return console.log("setted vs weights");
      });
    });
    socket.on("start", function() {
      return start();
    });
    return socket.on("play", function(data) {
      return requestParallel("play", data).then(function() {
        return console.log("to play...");
      });
    });
  });

  server.listen(8000);

  start = function() {
    var n;
    n = function(a) {
      return a.split("").map(Number);
    };
    return Promise.resolve().then(function() {
      return requestParallel("ready", {
        length: 12,
        seed: n("111000011001"),
        carrier_freq: 2000,
        isChirp: false,
        powL: 10,
        PULSE_N: 1
      });
    }).then(function() {
      return log("sockets", io.of('/node').sockets.map(function(socket) {
        return socket.id;
      }));
    }).then(function() {
      return requestParallel("startRec");
    }).then(function() {
      var a, prms;
      prms = io.of('/node').sockets.map(function(socket) {
        return function() {
          return Promise.resolve().then(function() {
            return requestParallel("startPulse", socket.id);
          }).then(function() {
            return request(socket, "beepPulse");
          }).then(function() {
            return log("beepPulse", socket.id);
          }).then(function() {
            return requestParallel("stopPulse", socket.id);
          });
        };
      });
      a = prms.reduce((function(a, b) {
        return a.then(function() {
          return b();
        });
      }), Promise.resolve());
      return a["catch"](function(err) {
        return error(err, err.stack);
      });
    }).then(function() {
      return requestParallel("stopRec");
    }).then(function() {
      return requestParallel("sendRec");
    }).then(function(datas) {
      var socket, sockets;
      sockets = io.of('/calc').sockets;
      socket = sockets[sockets.length - 1];
      console.log("preCalc", datas, socket.id);
      return request(socket, "calc", datas);
    }).then(function() {
      return console.info("end");
    })["catch"](function(err) {
      return console.error(err, err.stack);
    });
  };

  request = function(socket, eventName, data) {
    return new Promise(function(resolve, reject) {
      socket.on(eventName, function(data) {
        socket.removeAllListeners(eventName);
        return resolve(data);
      });
      return socket.emit(eventName, data);
    });
  };

  requestParallel = function(eventName, data) {
    var prms;
    log("requestParallel", eventName);
    prms = io.of('/node').sockets.map(function(socket) {
      return request(socket, eventName, data);
    });
    return Promise.all(prms);
  };

  requestLinear = function(eventName) {
    var prms;
    log("requestLinear", eventName);
    prms = io.of('/node').sockets.map(function(socket) {
      return function() {
        return request(socket, eventName, data);
      };
    });
    return prms.reduce((function(a, b) {
      return a.then(function() {
        return b();
      });
    }), Promise.resolve());
  };

}).call(this);
